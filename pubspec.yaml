name: newdb
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
    intl: ^0.18.1  # or latest version


  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  intl_phone_field: ^3.1.0  # ✅ Added for phone input with flag support

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.jpg